extend list.jade
block nav
    table.table.table-striped
      // 行
      tr
        th 姓名
        // 单元格
        th 职业
        th 年龄
        // 单元格
        th 借阅时间
        th 书名
        // 单元格
        th 归还时间

        - for(i in userlist)
           tr
           - for(j in userlist[i])
              td=userlist[i][j]
        - for(x=0;x<15;x++)
           tr
            - for(y=0;y<6;y++)
              td --
      script.
        var x = jQuery.isEmptyObject(userlist)
        if(x==true){
          alert("haha")
        }